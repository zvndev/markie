# Current task — Markie

No active implementation task is selected during initialization.

The first wakeup should re-ground, run `./init.sh`, then select the highest-priority unfinished
entry from `feature_list.json`: `F-001`.
