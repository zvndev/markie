# Current task — Markie

No active implementation task is selected in this planning update.

The next automated wakeup should re-ground, run `./init.sh`, then select the highest-priority
unfinished feature from `feature_list.json`: `F-006`, the repeatable light-mode visual audit.
